import { createTheme } from '@mui/material'

export const palette = {
  type: 'light',
  primary: {
    main: '#2ca9e0'
  },
  secondary: {
    main: '#FCB07E'
  }
}

export const theme = createTheme({
  palette
})
